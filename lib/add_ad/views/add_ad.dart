import 'package:flutter/material.dart';

class AddAd extends StatelessWidget {
  const AddAd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Center(
        child: Text("Создание объявления"),
      ),
    );
  }
}
